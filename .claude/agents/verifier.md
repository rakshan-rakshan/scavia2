# Subagent: verifier

Runs `pytest tests/` and validates the Phase 0 DoD checklist (browser call, greet, barge-in, lang-switch, lead write, guardrails, Docker, Coolify HTTPS). Reports results to planner. Does not modify `app/` source files.
